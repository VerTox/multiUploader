# multiUploader

Кроссплатформенное GUI приложение для загрузки файлов на различные файловые хостинги.

## Статус проекта

🔄 **В разработке** - Базовый UI готов, идет тестирование с мок провайдерами

## Возможности

- ✅ Кроссплатформенный GUI (Windows, macOS, Linux)
- ✅ Поддержка батчевой загрузки файлов (chunked upload)
- ✅ Отображение прогресса загрузки в реальном времени
- ✅ Индикация скорости загрузки
- ✅ Расчет оставшегося времени (ETA)
- ✅ Настройка размера батча (chunk size)
- ✅ Управление провайдерами (включение/выключение)
- ✅ Хранение API ключей
- 📋 Поддержка 4 провайдеров (в разработке)

## Поддерживаемые провайдеры

| Провайдер | Статус | Документация |
|-----------|--------|--------------|
| Rootz.so | 📋 Планируется | https://www.rootz.so/docs |
| DataVaults.co | 📋 Планируется | https://datavaults.co/pages/api |
| AkiraBox.com | 📋 Планируется | https://akirabox.com/api |
| FileKeeper.net | 📋 Планируется | https://datanodes.docs.apiary.io/# |
| **Mock Providers** | ✅ Для тестирования | - |

## Сборка и запуск

### Требования

- Go 1.25 или новее
- Fyne v2.7.1 (устанавливается автоматически через `go mod`)

### Сборка

```bash
# Клонировать репозиторий
cd multiUploader

# Скачать зависимости
go mod download

# Собрать приложение
go build -o multiUploader main.go

# Запустить
./multiUploader
```

### Режим разработки

```bash
# Запуск без сборки
go run main.go
```

## Тестирование UI

В текущей версии приложение использует **мок провайдеры** для тестирования UI:

### Доступные мок провайдеры

1. **Mock Fast (10 MB/s)** - быстрая загрузка
2. **Mock Medium (2 MB/s)** - средняя скорость
3. **Mock Slow (1 MB/s)** - медленная загрузка

### Как протестировать

1. **Запустите приложение:**
   ```bash
   ./multiUploader
   ```

2. **Перейдите на вкладку Settings:**
   - Установите "Default Chunk Size" (например, 5 MB)
   - Включите один или несколько мок провайдеров
   - Введите любой API ключ (минимум 10 символов, например: `test-api-key-12345`)
   - Нажмите "Save Settings"

3. **Перейдите на вкладку Upload:**
   - Выберите провайдер из dropdown
   - Нажмите "Select File" и выберите любой файл
   - Нажмите "Upload"

4. **Наблюдайте за прогрессом:**
   - Progress bar показывает процент выполнения
   - "Uploaded" показывает загруженный объем / общий размер
   - "Speed" показывает текущую скорость загрузки
   - "ETA" показывает оставшееся время

5. **Тестирование отмены:**
   - Во время загрузки нажмите "Cancel"
   - Загрузка должна остановиться

### Что тестировать

- ✅ Отображение прогресса загрузки
- ✅ Расчет и отображение скорости
- ✅ Расчет ETA
- ✅ Отмена загрузки
- ✅ Сохранение настроек
- ✅ Переключение между провайдерами
- ✅ Изменение chunk size
- ✅ Валидация API ключа

## Структура проекта

```
multiUploader/
├── main.go                          # Точка входа
├── internal/
│   ├── ui/
│   │   ├── app.go                  # Менеджер приложения
│   │   ├── upload_tab.go           # Вкладка загрузки
│   │   └── settings_tab.go         # Вкладка настроек
│   ├── providers/
│   │   ├── provider.go             # Interface провайдера
│   │   ├── progress.go             # Система прогресса
│   │   ├── defaults.go             # Константы
│   │   └── mock.go                 # Мок провайдер
│   └── config/
│       └── config.go               # Менеджер конфигурации
├── PLAN.md                         # План разработки
├── PROVIDERS.md                    # Документация провайдеров
└── README.md                       # Этот файл
```

## Хранение настроек

Настройки приложения хранятся в зависимости от ОС:

- **Linux:** `~/.config/multiUploader/`
- **macOS:** `~/Library/Preferences/multiUploader/`
- **Windows:** `%APPDATA%\multiUploader\`

## Технологии

- **Go 1.25+** - язык программирования
- **Fyne v2.7.1** - кроссплатформенный GUI framework
- **Fyne Preferences API** - кроссплатформенное хранение настроек

## Разработка

### Добавление нового провайдера

1. Создайте новый файл в `internal/providers/` (например, `newprovider.go`)
2. Реализуйте интерфейс `Provider`:
   ```go
   type Provider interface {
       Name() string
       Upload(ctx, file, filename, fileSize, chunkSize, progress chan) (*UploadResult, error)
       RequiresAuth() bool
       ValidateAPIKey(apiKey string) error
       SupportsChunkedUpload() bool
       DefaultChunkSize() int64
   }
   ```
3. Зарегистрируйте провайдер в `main.go`:
   ```go
   multiApp.RegisterProvider(providers.NewYourProvider())
   ```

### Архитектура

Проект следует многослойной архитектуре:

- **UI Layer** (Fyne) - графический интерфейс
- **Business Logic Layer** - провайдеры и их реализации
- **Configuration Layer** - управление настройками

Подробнее см. [PLAN.md](PLAN.md)

## Roadmap

- [x] Базовый UI с вкладками
- [x] Система прогресса загрузки
- [x] Конфигурация и настройки
- [x] Мок провайдеры для тестирования
- [ ] Реализация Rootz.so provider
- [ ] Реализация DataVaults.co provider
- [ ] Реализация AkiraBox.com provider
- [ ] Реализация FileKeeper.net provider
- [ ] Тестирование на всех платформах
- [ ] Релиз v1.0

## Лицензия

MIT License

## Контакты

Вопросы и предложения - создавайте issue в репозитории